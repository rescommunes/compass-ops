# update the values in this file then rename it to `local.secrets.yaml`
# see readme for more info on encrypting with sops
# run the script `./secrets.sh dev encrypt`
env:
    secrets:
      enabled: true
      ## choose a dns provider and uncomment the relevant section by removing the leading '#'

      # route53:
      #   accessKey: This is your secret access key for your DNS provider used for cert-manager DNS-01 challenges

      # cloudflare:
      #   # in "User Profile > API Tokens > API Tokens", set permissions "Zone-DNS-Edit" and "Zone-Zone-Read"
      #   apiToken: This is your Cloudflare API token with DNS edit permissions

      # clouddns:
      #   serviceAccountJson: |
      #     {
      #       "type": "service_account",
      #       "project_id": "example-project",
      #       "private_key_id": "1234567890abcdef1234567890abcdef12345678",
      #       "private_key": "-----BEGIN PRIVATE KEY-----\nMIIEvAIBADANBgkqhkiG9w0BAQEFAASCBKYwggSiAgEAAoIBAQCu...\n-----END PRIVATE KEY-----\n",
      #       "client_email": "cert-manager@example-project.iam.gserviceaccount.com",
      #       "client_id": "123456789012345678901",
      #       "auth_uri": "https://accounts.google.com/o/oauth2/auth",
      #       "token_uri": "https://oauth2.googleapis.com/token",
      #       "auth_provider_x509_cert_url": "https://www.googleapis.com/oauth2/v1/certs",
      #       "client_x509_cert_url": "https://www.googleapis.com/robot/v1/metadata/x509/cert-manager%40example-project.iam.gserviceaccount.com"
      #     }

      # azuredns:
      #   clientSecret: This is your Azure client secret for your DNS provider used for cert